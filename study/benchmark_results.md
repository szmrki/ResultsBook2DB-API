# 演習1: PostgreSQL vs Spark ベンチマーク結果

演習書 `memo/learn/spark_airflow_exercises.html` §6 の測定記録。

**目的**: `memo/olap_analytics_plan.md` の「着手判断は実測から入ること」を満たす。
列指向（OLAP）層を作る価値があるかを、推測ではなく数字で判断する材料にする。

最終更新: 2026-08-12 / 状態: **全4経路の測定完了**

---

## 測定環境

| 項目 | 値 |
|---|---|
| CPU | Intel Core i7-14700KF（28 論理コア） |
| メモリ | 15 GB |
| OS | WSL2 (Ubuntu 24.04) |
| Java | OpenJDK 17.0.19 |
| PySpark | 4.2.0 |
| PostgreSQL | 18.4（Docker コンテナ、ポート5432を公開） |
| 対象データ | `rb2db_four` の `stones`（1,152,837 行 / 9 列） |

**測定方法**: 各条件を4回実行し、**1回目をウォームアップとして捨てて**残り3回の中央値を採用
（`study/02_benchmark.py` の `measure()`）。

---

## Q1: stones 全件の x, y の平均・標準偏差

```sql
SELECT AVG(x), STDDEV(x), AVG(y), STDDEV(y) FROM stones
```

### 結果

| 経路 | 中央値 | 1分割比 | PG 比 |
|---|---:|---:|---:|
| **経路1: PostgreSQL** | **0.023 秒** | — | 1.0× |
| 経路2: Spark + JDBC（1分割） | 0.742 秒 | 1.0× | 32× 遅い |
| 経路2: Spark + JDBC（4分割） | 0.276 秒 | 2.7× 速い | 12× 遅い |
| 経路2: Spark + JDBC（8分割） | 0.194 秒 | **3.8× 速い** | 8.4× 遅い |
| 経路3: Spark + Parquet | 0.163 秒 | — | 7.1× 遅い |
| 経路4: DuckDB + Parquet | **0.004 秒** | — | **6.5× 速い** |

結果の一致（`math.isclose(rel_tol=1e-9)`）は全経路で **OK**。

2回測定して再現性を確認（ブレは1割以内）:

| 経路 | 1回目 | 2回目 |
|---|---:|---:|
| PostgreSQL | 0.025 | 0.023 |
| Spark 1分割 | 0.727 | 0.742 |
| Spark 4分割 | 0.258 | 0.276 |
| Spark 8分割 | 0.183 | 0.194 |

### 考察

**1. 並列度の効果は明確に出た（演習の主目的）。**

1分割 → 8分割で **3.8倍**。演習書 §2-2 の「1パーティションしかなければ 28 コアあっても
1コアしか働かない」が、そのまま数字で確認できた。

伸びは 4分割で 2.7倍、8分割で 3.8倍と**逓減している**。分割数に比例はしない。
JDBC 接続の確立コストと、シャッフルでの集約コストが分割数に応じて増えるため。
→ 最適値は16分割（後述の §6-6 探索結果）。

**2. PostgreSQL が Spark より速い — ただし理由は「Spark が遅い」ではない。**

8.4倍の差がついたが、これは演習書 §6-2 で予告した通りの結果であり異常ではない。
ただし、その要因を実行計画から確認したところ、**予想より PostgreSQL が有利な条件が揃っていた**。

```
Finalize Aggregate (actual time=1004.979..1006.396 rows=1)
  Buffers: shared hit=11875           ← 全てキャッシュヒット。ディスクI/O ゼロ
  -> Gather  Workers Planned: 2       ← PostgreSQL 自身も並列実行している
     -> Parallel Seq Scan on stones (rows=384279 loops=3)
```

判明した2点:

- **PostgreSQL も並列スキャンしていた**（ワーカー2つ + 親で3並列）。
  「PG=逐次 vs Spark=並列」という単純な対比ではなかった。
- **データが全てバッファキャッシュに載っていた**（`shared hit=11875`, `read=0`）。
  157万行 × 該当列は約 93MB で、shared_buffers に収まる規模。
  **ディスクI/O が発生していない**ため、行指向の不利（不要な列も読む）が現れにくい。

→ **この条件下では、列指向の利点が最も出にくい。**
Spark 側は JDBC 転送と JVM のオーバーヘッドを丸ごと負う一方、PG 側はメモリ上で
並列集計するだけで済んでいる。

**3. Exchange（シャッフル）は出るが、コストは小さい。**

```
HashAggregate(functions=[avg(x)])              ← 最終集計
  Exchange SinglePartition                     ← シャッフル
    HashAggregate(functions=[partial_avg(x)])  ← 各パーティションで部分集計
      Scan JDBCRelation
```

`groupBy` がないのに `Exchange` が出るのは、Spark の集計が**常に2段構成**のため
（部分集計 → 集約）。分割数が1でも計画は同じ形になる。

転送されるのは**部分集計の結果だけ**（157万行ではない）なので、実コストは極小。
Q2（`groupBy`）では `hashpartitioning` に変わり、性質が変わるはず。

---

## Q2: shot_order 別の平均距離（GROUP BY）

```sql
SELECT shot_order, AVG(distance_from_center)
FROM stones
WHERE shot_order IS NOT NULL AND shot_order > 0
GROUP BY shot_order ORDER BY shot_order
```

### 結果

| 経路 | 中央値 | Q1 との比 |
|---|---:|---:|
| 経路1: PostgreSQL | 0.038 秒 | 1.6× 遅い |
| 経路2: Spark + JDBC（1分割） | 0.598 秒 | **0.8×（速い）** |
| 経路2: Spark + JDBC（4分割） | 0.255 秒 | 0.9× |
| 経路2: Spark + JDBC（8分割） | 0.191 秒 | 1.0× |

一致確認は全経路 **OK**（16行 × 2列）。

### 考察

**1. シャッフルが2回に増えたが、Q1 より速い。**

```
Exchange rangepartitioning(shot_order, 200)   ← orderBy のため
Exchange hashpartitioning(shot_order, 200)    ← groupBy のため
```

Q1 の `Exchange SinglePartition` 1回に対し、Q2 は2回。にもかかわらず 1分割で
Q1 より速い（0.598 vs 0.744 秒）。**シャッフル回数だけでは速度は決まらない。**

理由は転送量。`PushedFilters` に NULL・負値の除外が降りており、
異常値 3,180 件（`sql_notes.md` §2）は**転送すらされていない**。

```
PushedFilters: [*IsNotNull(shot_order), *GreaterThan(shot_order,0)]
ReadSchema: struct<distance_from_center:double,shot_order:int>
```

**2. PostgreSQL 側は Q1 より遅くなった**（0.024 → 0.038 秒）。
GROUP BY のハッシュ集計が加わったぶん。ただし依然として Spark の5倍速い。

**3. 並列度の効果は Q1 と同傾向**（1→8分割で 3.1倍）。
Q1 の 3.8倍よりやや小さいのは、シャッフルのコストが分割数に応じて増えるため。

---

## Q3: event 別 × shot_order 別の件数（4段 JOIN）

`stones → shots → ends → games → events` を JOIN し、大会名と shot_order で集計。

### 結果

| 経路 | 中央値 | Q1 との比 |
|---|---:|---:|
| 経路1: PostgreSQL | 0.139 秒 | 5.8× 遅い |
| 経路2: Spark + JDBC（1分割） | 0.899 秒 | 1.2× 遅い |
| 経路2: Spark + JDBC（4分割） | **0.598 秒** | 2.3× 遅い |
| 経路2: Spark + JDBC（8分割） | 0.631 秒 | 2.4× 遅い |

一致確認は全経路 **OK**。

### 考察

**1. 8分割が4分割より遅い（初めての逆転）。**

Q1・Q2 では分割を増やすほど速くなったが、Q3 は **4分割が最速**で8分割は悪化した。
分割が効くのは `stones` の読み込みだけで、**シャッフルのコストは分割数に応じて増える**ため。
「分割は多いほど良い」ではないことの実例（§6-6 の最適値探索が示すテーマ）。

**2. Exchange が 10 回。JOIN 1回につき2回発生する。**

JOIN 4回 × 2（両側を同じキーで再配置）+ groupBy 1 + orderBy 1 = 10。
Q1 の1回、Q2 の2回に対して桁違いで、これが Q3 が遅い主因。

**3. 全ての JOIN が SortMergeJoin。Broadcast は選ばれなかった。**

`events` は42行しかないため BroadcastHashJoin（小さい表を配ってシャッフルを回避）を
期待したが、実際は `SortMergeJoin`。**JDBC 経由では Spark が行数を事前に知らない**ため、
「小さいから配る」判断ができない。

→ **Parquet は統計情報を持つので、経路3では Broadcast に変わる可能性がある。**
演習3で比較する価値あり。

**4. PushedFilters が全テーブルに効いている。**

```
stones: [*IsNotNull(shot_order), *GreaterThan(shot_order,0), *IsNotNull(shot_id)]
shots : [*IsNotNull(id), *IsNotNull(end_id)]
```

書いていない `IsNotNull` が自動追加されている。JOIN キーが NULL の行は結果に出ないため、
Spark が先回りして除外している。読む列も最小限（stones は9列中2列）。

---

## Q4: inhouse=1 に絞った座標集計（述語プッシュダウン）

```sql
SELECT AVG(x), AVG(y), COUNT(*) FROM stones WHERE inhouse = 1
```

Q1 と同じ形（`groupBy` なし、`Exchange` 1回）で、違いは `WHERE` の有無だけ。

### 結果

| | Q1（全件） | Q4（inhouse=1） | 差 |
|---|---:|---:|---:|
| 対象行数 | 1,152,837 | 約 814,000（70.6%） | −29% |
| 経路1: PostgreSQL | 0.024 | 0.026 | **+8%** |
| 経路2: Spark（1分割） | 0.744 | 0.502 | **−33%** |
| 経路2: Spark（4分割） | 0.263 | 0.214 | −19% |
| 経路2: Spark（8分割） | 0.188 | 0.153 | −19% |

一致確認は全経路 **OK**。

### 考察

**1. 述語プッシュダウンの効果が数字で出た。**

```
PushedFilters: [*IsNotNull(inhouse), *EqualTo(inhouse,1)]
```

絞り込みが PostgreSQL 側で適用されるため、除外された約34万行は
**JDBC を通って転送されていない**。読む行数 −29% に対し実行時間 −33% と、
ほぼ比例している。

**2. PostgreSQL だけ速くならなかった**（0.024 → 0.026 秒）。

全データがバッファキャッシュに載っている（Q1 の考察2）ため「読む行数が減る」利得が小さく、
157万行すべてに `inhouse = 1` を判定するコストが上回った。

→ **Spark は転送が律速、PostgreSQL は評価が律速**という違いが、
同じクエリで対照的に現れた。

---

## numPartitions の最適値探索（演習書 §6-6）

全クエリを 1/2/4/8/16/32 分割で測定。バーは各クエリ内での相対値。

```
Q1: 全件の平均・標準偏差
  1分割 ################################## 0.734
  2分割 ###################                0.413
  4分割 ############                       0.251
  8分割 #########                          0.188
 16分割 #######                            0.149  <- 最速
 32分割 #######                            0.161

Q2: shot_order 別 (GROUP BY)
  1分割 ################################## 0.581
  2分割 #####################              0.363
  4分割 ##############                     0.237
  8分割 ##########                         0.173
 16分割 #########                          0.162  <- 最速
 32分割 ###########                        0.184

Q3: 4段 JOIN
  1分割 ###########################        0.816
  2分割 ####################               0.612
  4分割 ##################                 0.546  <- 最速
  8分割 ###################                0.567
 16分割 ########################           0.730
 32分割 ################################## 1.030  ← 1分割より遅い

Q4: inhouse=1 の絞り込み
  1分割 ################################## 0.451
  2分割 ####################               0.260
  4分割 ############                       0.164
  8分割 #########                          0.123
 16分割 ########                           0.107  <- 最速
 32分割 #########                          0.121
```

### 考察

**1. 全クエリに最適値が存在し、超えると悪化する。**

| クエリ | 最適分割数 | 1分割比 |
|---|---:|---:|
| Q1 | 16 | 4.9× 速い |
| Q2 | 16 | 3.6× 速い |
| **Q3** | **4** | 1.5× 速い |
| Q4 | 16 | 4.2× 速い |

「分割は多いほど良い」は誤り。**U字カーブを描く。**

**2. Q3 だけ最適値が桁違いに小さい（4分割）。**

シャッフルが10回あるため（Q3 の考察2）、分割を増やすとシャッフル対象も増える。
32分割では **1分割より遅くなった**（1.030 vs 0.816 秒）。
**並列化がマイナスに転じる**実例。

→ **最適値はクエリの形に依存する。** 一律に設定できない。

**3. 28コアあるのに16分割で頭打ち。**

CPU が足りないのではなく、**PostgreSQL 側の同時接続が律速**と考えられる。
JDBC の分割は「DB への接続を増やす」ことなので、Spark 側の並列度だけでは決まらない。

---

## 演習3: Parquet の生成（演習書 §8-2）

`stones` に `event_id` / `category` / `game_id` を JOIN で付与して書き出し
（`study/03_export_parquet.py`）。分析側が JOIN 不要で使えるようにする。

**測定方法**: 書き込みはディスクI/O のブレが大きいため、
`export()` も `measure()` と同じく4回実行して1回目を捨て、中央値を採用。

### パーティション設計の比較

| 設計 | 時間（中央値） | 計測値 | サイズ | ファイル数 | 平均サイズ |
|---|---:|---|---:|---:|---:|
| 分割なし | 0.82 秒 | 0.75, 0.82, 0.83 | 34.96 MB | 37 | 967.6 KB |
| `category`（4分割） | 0.61 秒 | 0.64, 0.60, 0.61 | 34.95 MB | 38 | 941.7 KB |
| **`event_id`（42分割）** | **0.53 秒** | 0.53, 0.56, 0.53 | 34.96 MB | 42 | 852.2 KB |
| `game_id`（1,891分割） | **2.21 秒** | 2.21, 2.12, 2.21 | **41.24 MB** | 1,891 | **22.3 KB** |

### 圧縮形式

| | サイズ | 時間 | 計測値 |
|---|---:|---:|---|
| snappy（既定） | 34.96 MB | 0.49 秒 | 0.52, 0.48, 0.49 |
| zstd | **29.49 MB** | 0.51 秒 | 0.52, 0.51, 0.51 |

### 考察

**1. 小ファイル問題が明確に出た（`game_id` 分割）。**

平均22.3KB のファイルが1,891個。`event_id`（852KB × 42）と比べて3点すべてが悪化:

- 書き込みが **4.2倍遅い**（0.53 → 2.21 秒）
- **サイズが18%増える**（34.96 → 41.24 MB）
- 読み取り時もファイルを開くコストが支配的になる

サイズ増加が本質的。**Parquet はファイルごとにヘッダ・フッタ・統計情報を持つ**ため、
1ファイルが小さいとメタデータの比率が上がる。圧縮も効きにくくなる（辞書が育たない）。

→ **パーティションは細かいほど良いのではない。** 適正粒度は数百KB〜数十MB。

**2. 適度な分割はむしろ速い（flat 0.82 → by_event 0.53 秒）。**

`event_id` で42分割したほうが、分割なしより **1.5倍速い**。
書き込み前にキーで集約されるため、1パーティション1ファイルにまとまるのが効く。
分割なしはメモリ上の37パーティションをそのまま書くので、細切れになる。

ただし `game_id` まで細かくすると逆転する。**適正粒度が存在する。**

**3. Parquet の圧縮効率。**

PostgreSQL 上で約93MB → snappy 34.96MB（**約63%減**）、zstd 29.49MB（**約68%減**）。
列指向で同じ列の値が並ぶため圧縮がよく効く。

zstd は16%小さく、書き込みは4%遅いだけ（0.49 → 0.51 秒）。
**トレードオフとして明確に割に合う。**

---

## 経路3: Spark + Parquet（演習書 §8-4）

`stones_by_event` を読んで Q1〜Q4 を再測定。
Parquet には `event_name` / `event_id` / `category` / `game_id` を持たせてある。

### 結果

| | Q1 | Q2 | **Q3** | Q4 |
|---|---:|---:|---:|---:|
| 経路1: PostgreSQL | 0.023 | 0.037 | **0.140** | 0.026 |
| 経路2: Spark + JDBC（最速分割） | 0.163 | 0.179 | 0.681 | 0.111 |
| 経路3: Spark + Parquet | 0.163 | 0.226 | **0.259** | 0.136 |

一致確認は全経路 **OK**。

### 考察

**1. 重いクエリでのみ列指向が効く。**

Q1・Q2・Q4 は経路2とほぼ同じか、やや遅い。一方 **Q3 だけ 2.6倍速くなった**
（0.681 → 0.259 秒）。

単純なクエリで差が出ないのは、データが約35MB でメモリに載るため
「読む量が減る」利点が現れないから（Q1 の考察2と同じ構図）。
**列指向が効くのは計算そのものが重いとき。**

**2. Q3 が速くなったのは Parquet の性能ではなく、データの持たせ方。**

実行計画の差:

| | 経路2 | 経路3 |
|---|---:|---:|
| SortMergeJoin | 4 | **0** |
| Exchange | 10 | **2** |
| 計画の行数 | 33 | 8 |

Parquet に `event_name` を含めたので、**4段 JOIN が丸ごと不要**になった。
残る `Exchange` は `groupBy` と `orderBy` の2回だけ。

→ **「Parquet 層に何を持たせるか」の設計判断が、エンジンの性能より効いた。**
非正規化して保存すれば、分析側は JOIN を書かずに済む。

**3. それでも経路1（PostgreSQL）は上回れなかった。**

Q3 で最も差が縮まったが、0.140 秒 vs 0.259 秒で **PostgreSQL が約1.9倍速い**。
Q1・Q2・Q4 では依然として5〜7倍の差。

---

## 経路4: DuckDB + Parquet（演習書 §8-6）

経路3と**同じ Parquet** を DuckDB で読む。エンジンだけを入れ替えた比較。

### 結果

| | Q1 | Q2 | Q3 | Q4 |
|---|---:|---:|---:|---:|
| 経路1: PostgreSQL | 0.026 | 0.036 | 0.127 | 0.027 |
| 経路2: Spark + JDBC | 0.171 | 0.181 | 0.706 | 0.110 |
| 経路3: Spark + Parquet | 0.171 | 0.190 | 0.197 | 0.148 |
| **経路4: DuckDB + Parquet** | **0.004** | **0.005** | **0.009** | **0.004** |

一致確認は全経路 **OK**。

**PostgreSQL の 6.5〜14倍、Spark の 22〜43倍速い。**

### 検算

4ミリ秒で157万行の集計は速すぎるため、Parquet のメタデータで
ショートカットしていないかを確認した。

```
SELECT COUNT(*)                  → 1,152,837        （全行ある）
SELECT COUNT(DISTINCT shot_id)   → 274,283 / 0.030秒
```

`COUNT(DISTINCT)` は27万個のハッシュ集合を構築するため、
**統計情報では答えられず全行を読む必要がある**。それが 0.030 秒。
Q1〜Q4 より遅いのはハッシュ構築のぶんで、辻褄が合う。→ **本物。**

（`COUNT(DISTINCT shot_id)` = 274,283 は `shots` の行数と一致。
全ストーンが正しく投球に紐付いていることも確認できた。）

### 考察

**1. 同じ Parquet でもエンジンで20倍以上違う。**

経路3と経路4はデータが完全に同じ。差はエンジンだけ。

- **オーバーヘッドがない** — JVM 起動なし、シリアライズなし、シャッフルなし。
  Spark が分散のために払うコストを、単一マシン特化の DuckDB は払わない
- **ベクトル化実行** — 1行ずつではなく1024行のバッチ単位で処理する

→ **この規模では「分散する」こと自体がコスト**（演習書 §1-6 の実証）。

**2. Q3 の差が最も小さい（14倍 → 実は最も健闘）。**

PostgreSQL 比で Q1 6.5倍、Q3 **14倍**。重いクエリほど差が開く。
JOIN を消した形で保存してあることが、DuckDB でも効いている。

**3. クエリの重さによらず 4〜9ミリ秒に収まる。**

PostgreSQL は Q1 0.026 → Q3 0.127 と5倍に増えるが、
DuckDB は 0.004 → 0.009 と2倍で済む。**重いクエリほど相対的に強い。**

---

## 結論（全4経路の測定を終えて）

### 全体比較

| | Q1 | Q2 | Q3 | Q4 |
|---|---:|---:|---:|---:|
| 経路1: PostgreSQL | 0.026 | 0.036 | 0.127 | 0.027 |
| 経路2: Spark + JDBC | 0.171 | 0.181 | 0.706 | 0.110 |
| 経路3: Spark + Parquet | 0.171 | 0.190 | 0.197 | 0.148 |
| **経路4: DuckDB + Parquet** | **0.004** | **0.005** | **0.009** | **0.004** |

### 1. `olap_analytics_plan.md` の着手条件への回答

当初の警告 —

> 「OLTP だから遅いはず」は決めつけ。150万行程度なら適切なインデックスで
> PostgreSQL でも案外速い可能性がある。

— は **Spark に対しては正しく、DuckDB に対しては誤り**だった。

- **対 Spark**: PostgreSQL が 4〜7倍速い。Parquet 化しても覆らない（経路3で1.9倍差）
- **対 DuckDB**: **DuckDB が 6.5〜14倍速い**

→ **B/C案（Parquet + DuckDB）に明確な根拠が出た。**

### 2. ただし「速いから作るべき」ではない

PostgreSQL の 0.127 秒も、**体感では待ち時間がない**。
0.009 秒との差は数字上は14倍でも、**人間には区別できない**。

作る判断をするなら、根拠は速度ではなく別のところにある。

- API サーバの起動・トンネル管理が不要になる（ファイルを直接読める）
- analytics 側が JOIN を書かずに済む（非正規化して保存できる）
- レート制限を受けない

**速度は「作っても遅くならない」ことの確認であって、作る動機そのものではない。**

### 3. 未検証の留保

**単発クエリしか測っていない。** analytics の実運用は「全大会横断で集計を何度も回す」形。
また今回はデータ35MB が完全にメモリに載る条件だった。
**キャッシュに載らない規模では、列指向の利点がさらに大きく出る可能性が高い**
（ディスクI/O が発生すれば「読む列だけ読む」が効くため）。

### 4. 副産物: 設計判断がエンジン選択より効いた場面

Q3 で経路2→経路3が 3.6倍改善したのは、Parquet が速いからではなく
**`event_name` を持たせて JOIN を不要にしたから**（`Exchange` 10回→2回）。

データの持たせ方は、エンジンの性能と独立に効く。

---

## 次にやること

- [x] Q2（`shot_order` 別 GROUP BY）— `Exchange hashpartitioning` を確認
- [x] Q3（4段 JOIN）— JOIN ごとのシャッフル数を確認
- [x] Q4（`inhouse=1` 絞り込み）— 述語プッシュダウンの効果
- [x] `numPartitions` の最適値探索（1/2/4/8/16/32）— 演習書 §6-6
- [x] Parquet の生成（演習書 §8-2）— パーティション設計と圧縮形式を比較
- [x] 経路3: Spark + Parquet（演習書 §8-4）
- [x] 経路4: DuckDB + Parquet（演習書 §8-6）
- [×] 全て揃ったら `memo/olap_analytics_plan.md` に結論を反映
